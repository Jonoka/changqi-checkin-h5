# Coding Agent 约定

## 1. 开始与接续

阅读顺序：本文件 → README.md → docs/PRD.md → docs/SPEC.md → docs/TASKS.md → docs/DEPLOY.md → 视觉参考说明与实际工程。

PRD 定义范围，SPEC 定义行为与验收，TASKS 只组织执行，不能新增产品规则。用户最新明确变更应同步到相关文档再实现；一般实现细节按现有约定推进，不反复询问已确认事项。确有冲突且会改变业务时，先指出具体冲突，不静默扩大范围。

先检查远端实际分支、HEAD、文件及相关开放 PR；有本地工作区时再检查 `git status --porcelain=v1 --untracked-files=all`。网页会话没有用户本地访问能力时如实记录，不伪造本地检查。目标仓库或目录未明确时，不改其他项目。

## 2. 范围与实现

默认单 Agent 顺序开发，不搭多 Agent 平台，不为每个小任务建立 PR 或要求确认。遵守已有仓库规则。先跑通一处打卡和领取闭环，再完善视觉。

不做管理后台、库存、员工账号、多活动、GPS、硬件点亮、照片审核、动态票据、反作弊平台、实时推送或复杂 CI。允许 DEPLOY 约定的单个 GitHub Actions 构建工作流；这不是新增业务平台。不要为了使用 Spec 增加 Spec 框架或工作流工具。

保留基本正确性：身份不串用；文件和记录保存成功才打卡；同地点不重复计数；领取只首次标记；失败不假成功；重进能恢复记录。微信要求的授权、签名和普通会话照常实现，不把必要接入步骤删掉。

复用现有组件。空项目沿用 SPEC 默认栈；首次安装查官方资料、确定兼容版本并保留锁文件。不要编造精确版本、写 `latest` 作为可复现版本，或用更换数据库掩盖 MySQL 未验证。

## 3. 验证与进度

每个任务都实际执行：读取现状 → 最小实现 → 运行检查 → 修复 → 更新 TASKS → 继续。

仅在 docs/TASKS.md 维护进度与接续记录，不再新建 PLAN_AGENT、IMPLEMENTATION_STATUS、第二份任务清单或冗长日报。DEPLOY 只记稳定操作约定。任务状态、Actions 运行结果、服务器部署及微信真机结果分开记录。

缺公众号、域名、服务器或真机时，列清外部缺项，继续不受影响的工作。模拟仅在开发环境显式开启，并显示“开发演示”；生产开启模拟时应拒绝启动或明确禁止该入口。不得伪造调用、截图、测试或部署结果。

GitHub 提交成功不代表用户 Windows 工作区已同步；只有实际检查过相应工作区才报告其状态。

## 4. 视觉

参考 assets/reference/ 中已认可的效果图，不重新探索风格。复用绿色、米白、暖黄色、乡村插画和镬耳式建筑元素。

效果图不是网页背景：去掉手机外框、系统状态栏和小程序胶囊，正文、按钮、地点名、进度和领取状态使用真实页面元素。图中旧文字、示例进度、二维码和伪造景物不能覆盖 SPEC。尤其保留正确地点名“苞榕”，仅允许上传现场照片。

缺独立底图时可复用可用局部插画或用占位图先开发，不能把整张截图当成可操作界面，也不虚构已取得实景照片或素材授权。

## 5. Git、环境与上线

保留未提交修改、全部未跟踪文件和被忽略的环境文件；不执行破坏性 reset、clean、强推，不删除或覆盖用户已有工作。需要同步时先比较，仅在安全条件下 fast-forward。

初始化仅作用于用户明确指定的仓库。已有内容先读取再修改，不自动另建替代仓库、修改可见性、选择开源许可证或合并 PR。普通提交按已授予权限执行。

真实 AppSecret、数据库密码、SESSION_SECRET、统计访问凭据、用户照片和生产数据库不能提交。`.env.example` 仅占位。上传目录、数据库与备份必须独立于部署包。

没有明确授权，不生产部署、不改公众号菜单或现有消息服务、不清理生产数据。允许先准备说明与脚本。每次报告只写实际改动、实际检查、未验证项与下一步。

## 6. 两种开发入口与同一发布流程

网页端通过已授权 GitHub 工具开发；本地 Agent 在电脑工作区开发。共用本仓库与 TASKS，默认一个执行端顺序推进。换端先读最新提交和接续信息，不把仓库提交、用户电脑文件与服务器运行版本视为天然同步。

构建统一在 GitHub 托管 runner 上执行，不在 1 核 1GB 生产机上构建或装自托管 runner。工作流、Dockerfile、Compose 和请求文件尚未实现时，只能写待办，不能宣称已经可用。可执行环境不足时把相关检查交给 Actions 或本地执行端；Actions 内临时 MySQL 的结果注明环境，不能冒充宝塔生产库已验证。

网页端有可用 dispatch 写接口时可直接触发；否则按 DEPLOY 通过一次构建请求文件提交触发同一工作流。本地可用 `gh workflow run`。普通代码/文档提交不附带构建请求；不要用反复改工作流或空提交碰运气触发，也不要为按需构建额外搭平台。连接权限或事件触发失败如实报告；不得绕过保护或声称已触发。

授权范围为本轮任务：仅要求改文档，不触发构建；要求构建不等于授权上线。已明确授权某次发布及其目标时，本地 Agent 连续执行下载、上传、加载、更新和检查，不重复索要同一授权。GitHub runner 不持有生产 SSH 密钥；默认由用户电脑中有 SSH 权限的 Agent 部署。网页端只有在确有相应工具和授权时才执行服务器操作，不能凭一句“已给权限”假定能连接。

交接最少提供源码 SHA、run ID/attempt、artifact 名称、镜像标签、检查与未验证项。下载成功后本地端不重新打包；服务器只 load 同一镜像、更新一个应用容器，复用宝塔 MySQL。镜像包和运行密码不提交，照片持久挂载。构建已提交/排队、镜像产物可下载、容器运行正常、微信闭环通过是不同状态，不得合并成“已完成”。
