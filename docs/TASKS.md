# TASKS · Coding Agent 执行清单

**v1.3 · 2026-09-21 · 单 Agent 顺序执行**

输入：[PRD.md](PRD.md)、[SPEC.md](SPEC.md)、[AGENTS.md](../AGENTS.md)、[视觉参考](../assets/reference/README.md)。本文件既是任务清单，也是唯一进度/接续记录，不再维护人工排期或另一份状态文档。

## 0. 当前真实状态

- 目标仓库：`Jonoka/changqi-checkin-h5`；已核对为空的私有仓库，初始化文档提交至 `main`。未开始应用开发。
- 基础初始化提交：`c5bd5888d993984e8d7cfcbd42e5c92416070d6f`（`.gitattributes`）；本文件所在材料提交及后续 HEAD 以 GitHub / `git rev-parse HEAD` 实际结果为准，不在文件内写入无法自引用的最终 SHA。
- 视觉原图：五张 PNG 尚未写入 GitHub；文件、大小和哈希见 `assets/reference/manifest.json`。配套原图 ZIP 已保留；补入前 A0–A3 可继续，A4 不得声称原图已入库或视觉验收通过。
- 用户本地工作区：未检查、未同步。
- MySQL / 微信 / 真机 / 服务器：均未执行联调或验证。
- 下一步：在明确选定的开发工作区读取本仓库，从 A0 开始；先跑通最小工程、配置与 MySQL，不重新生成需求文档。
- 部署授权 / 部署结果：未获得、未部署。

## 1. 执行顺序与完成规则

| 任务 | 目标 | 依赖 | 当前状态 |
| --- | --- | --- | --- |
| A0 | 最小工程、配置、MySQL | 明确目标仓库 | 未开始 |
| A1 | 微信身份、两类扫码入口 | A0；真实账号/域名用于真机 | 未开始 |
| A2 | 一处照片打卡与进度恢复 | A0、扫码接口；本地可用开发模拟 | 未开始 |
| A3 | 两种领取与人数 | A2 | 未开始 |
| A4 | 已认可视觉与真实页面状态 | A2、A3 | 未开始 |
| A5 | 必要回归、问题修复 | A1–A4 | 未开始 |
| A6 | 授权部署与正式二维码 | A5、正式域名/服务器及授权 | 未开始 |

状态使用“未开始 / 进行中 / 本地通过待真机 / 阻塞 / 完成”。按实际依赖顺序继续，不每个小步骤都要求批准；外部条件缺失只阻塞对应检查，本地可实现部分继续。开发环境模拟与微信真机结果严格分开。

一个任务只有实现和对应检查都满足才算完成；没有 MySQL 不得用 SQLite 测试代替 MySQL 通过；没有真机不得用模拟扫码冒称微信通过。默认用轻量测试脚本，不引入 CI 平台或覆盖率指标。

## 2. 可执行任务

### [ ] A0 · 初始化可运行的最小工程

**输入：**目标仓库实际内容、SPEC S01/S08、环境样例。

**动作：**检查分支/HEAD/全部未跟踪文件，保留已有内容。空工程按默认栈建立 web、server、配置、db、scripts 等必要目录，优先根 npm 项目和单锁文件；已有工程复用。核对官方支持与依赖兼容版本，实际安装并锁定。实现配置校验、公开活动接口、MySQL 建表与连接。只有 users/checkins 两张业务表及会话组件必要技术表。

**产出：**真实可用的安装/开发/构建命令；锁文件；`config/activity.json`；`db/schema.sql`；`.env.example` 与 README 的运行说明。不提交真实凭据。

**检查：**项目启动、构建、数据库连接和建表实际执行；AC-01 的配置检查通过。数据库未接通时继续可做部分，但不得把 A0 的数据库检查标为完成。

### [ ] A1 · 微信授权与扫码入口

**输入：**A0；SPEC S02/S03/S08；实际公众号与域名条件。

**动作：**先核对实际账号网页授权/JS-SDK 能力及必要域名配置；实现授权、普通会话、JS-SDK 配置、引导页、扫码解析与普通会话 scannedPointKey。两用户隔离；重进恢复身份；直接扫 /q/ 永远引导；页内取得结果再进入上传。

**产出：**授权与扫码接口、公众号引导页、错误提示；必要的开发模拟入口（仅显式开发模式，不能生产开放）。

**检查：**AC-02～AC-04。缺凭据/域名/真机时列一次最小缺项，状态记“本地通过待真机”或“阻塞”，继续 A2–A4；不修改用户已有公众号消息服务或菜单。

### [ ] A2 · 先跑通一处照片打卡

**输入：**A0 的 MySQL 和配置、A1 扫码接口（本地允许开发模拟）；SPEC S04/S05。

**动作：**实现一张图片选择、预览、重选、提交、处理和持久保存；数据库确认后更新进度。重复不覆盖、失败不计数、超时先查状态。本人图片仅本人读取。先用一处完整跑通，再覆盖全部配置地点，不新增另一种打卡方式。

**产出：**上传页、本人进度接口、照片读取接口和实际保存逻辑。

**检查：**AC-05～AC-08 中可本地验证项；真机拍照/相册另记。至少有一张实际照片保存到 MySQL 对应的持久文件，并能刷新恢复。

### [ ] A3 · 领取闭环和人数

**输入：**A2 完成判断；SPEC S06/S07/S08。

**动作：**完成后展示游客领取码与自行确认按钮；派发页扫码打开只读，点击才提交。两个入口调用同一 markClaimed，保留首次时间/渠道。人数页直接 COUNT，由现成固定凭据保护；没有后台、库存或员工账号。

**产出：**本人领取、公开持码派发、只读人数，以及接口级重复确认测试。

**检查：**AC-09～AC-11。两个独立测试用户全部完成后分别通过不同路径领取；同一用户并发两路请求，计数只增加 1。先在隔离测试库检查，不清空生产数据库。

### [ ] A4 · 应用视觉与完整页面状态

**输入：**A2/A3 接口；SPEC S09；assets/reference/README.md 及五张参考图。

**动作：**复用认可风格，不重新探索。首页/示意地图可合并，成功可用弹层；实现真实文字、动态数字和按钮，去除手机框与小程序胶囊。替换图中旧装置文案、错误地名、错误进度和假二维码。缺独立插画可先用局部素材，不阻塞业务实现。

**产出：**首页/进度、地点上传、完成领取、引导、派发、人数页面的可交互版本及关键状态。

**检查：**AC-12；本地检查至少两种手机宽度，无横向溢出、按钮可用。用真实接口数据，不把整张截图铺成所谓完成页面。

### [ ] A5 · 回归、修复和真实记录

**输入：**A1–A4；SPEC AC-01～AC-14。

**动作：**运行项目已有测试或轻量冒烟脚本，覆盖配置、用户隔离、照片失败、重复打卡、完成判定、两路领取和统计；逐项修复主流程问题并复测。微信返回页面再次扫码、权限/取消、真实相册和纸样用真机检查。

**产出：**必要测试脚本、修复代码，以及本文件中的真实检查结果。记录实际命令、环境、结果，不以“测试已编写”当通过。

**阻塞交付：**无法进入/扫码/上传/领取、串用户、重复计数、失败假成功、重进丢数据、部署覆盖照片。装饰和动画可简化；基础检查不能删。生产相关 AC-13/AC-14 在 A6 补完，缺外部条件保持未验证。

### [ ] A6 · 部署准备、授权上线与物料

**输入：**A5 结果；SPEC S10；正式域名/服务器和明确部署授权。

**动作：**先准备最短部署说明和可复用命令；取得授权再部署同域 HTTPS、应用服务、持久目录和既有备份。环境变量不进 Git，代码回退不恢复旧游客数据。公众号菜单变更另需授权，不覆盖其他菜单或服务。

**物料：**实现读取活动配置生成 PNG/SVG 及名称/URL 清单的脚本；测试码单独标识。正式域名未核验不生成冒称正式的印刷包；逐地点扫码核对后交付样张，不印个人领取码。

**产出：**源代码/锁文件、建表脚本、环境样例、简短部署说明、可复用二维码脚本；具备条件并实际完成后交正式二维码包和真实上线记录。

**检查：**AC-13/AC-14 及上线后的核心闭环复查。未授权时以“部署准备完成、未部署”交接，不编造已上线。

## 3. 验收结果表（执行时更新，不凭推测勾选）

| 验收项 | 当前结果 | 证据 / 未验证原因 |
| --- | --- | --- |
| AC-01 | 未验证 | 尚未实现 |
| AC-02 | 未验证 | 尚未实现/无微信真机记录 |
| AC-03 | 未验证 | 尚未实现/无微信真机记录 |
| AC-04 | 未验证 | 尚未实现/无微信真机记录 |
| AC-05 | 未验证 | 尚未实现/无真实选图记录 |
| AC-06 | 未验证 | 尚未实现 |
| AC-07 | 未验证 | 尚未实现 |
| AC-08 | 未验证 | 尚未实现 |
| AC-09 | 未验证 | 尚未实现 |
| AC-10 | 未验证 | 尚未实现 |
| AC-11 | 未验证 | 尚未实现 |
| AC-12 | 未验证 | 参考图不是实际网页 |
| AC-13 | 未验证 | 尚未实现/未部署 |
| AC-14 | 未验证 | 正式域名及印刷样张未就绪 |

可分开写“本地已通过；微信真机未验证”。本地命令输出与真机用户反馈要标明来源，不将两者混为一个“全绿”。

## 4. 接续记录（只维护此处）

每个执行任务结束时更新顶部状态、任务表、验收表和下面的当前摘要。避免重复长篇历史；需要的命令输出可放不含敏感数据的测试报告，并在此引用。

```text
实际日期：
仓库 / 分支 / HEAD：
本轮完成：任务编号、具体文件或功能
实际检查：命令、环境、退出结果；真机由谁操作、对应版本
未通过 / 未验证：具体项目及原因
外部缺项：只列确实缺少的资料或授权
下一步：第一个具备条件的未完成任务和动作
部署状态：未部署 / 测试部署 / 已授权生产部署
```

初始化阶段摘要：用户已指定 `Jonoka/changqi-checkin-h5`，已核对为空的私有仓库；本轮提交文档、约定、环境样例、视觉说明和原图清单。不包含应用代码，A0–A6 保持未开始、AC-01～AC-14 保持未验证。原图未入 GitHub；没有修改服务器、公众号、数据库或用户本地工作区。

本轮材料检查：原始 ZIP 完整性；三份文档读取；Markdown 相对链接与验收编号对应；初始化清单与环境占位值；五张原图解码、大小和哈希。上述检查只证明材料可交付，不代替应用测试。
